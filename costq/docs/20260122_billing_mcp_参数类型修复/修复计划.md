# Billing-Cost-Management-MCP-Server 参数类型修复计划

## 📋 问题背景

**问题描述**: `compute-optimizer` 工具的 `max_results` 参数出现类型验证错误

**错误信息**:
```
JsonSchemaException - Parameter validation failed: Invalid request parameters
Field 'max_results' has invalid type: $.max_results: string found, integer expected
```

**根本原因**:
根据 `costq/docs/20260117_MCP-Gateway-适配改造分析/02_改造步骤指南.md` 中 Step 8 的分析:
1. **FastMCP Schema 生成缺陷**: `int` 类型可能无法生成正确的 `type: integer` Schema
2. **Gateway 序列化行为不确定**: 当 Schema 不完整时,Gateway 可能将整数序列化为字符串
3. **Pydantic 验证拦截**: 函数签名定义为 `int`,但接收到 `str` 导致 ValidationError

## 🎯 修复方案

### 核心原则
**使用 Union[str, int] 类型 + 智能转换**,同时支持字符串和整数输入

### 参考代码
参考 `costq-risp-mcp-server` 的修复方案:
- `costq/docs/20260120_filter_expression分析/问题总结与修复方案.md`
- `src/costq-risp-mcp-server/handlers/sp_handler.py` (已修复)

---

## 📊 影响范围分析

### 1. 涉及的 Tools 文件

通过扫描发现,以下 tools 文件包含 `max_results: Optional[int]` 参数:

| 文件名 | 受影响的函数 | 参数名 | 行号 |
|--------|------------|-------|------|
| `compute_optimizer_tools.py` | `compute_optimizer` | `max_results` | 79 |
| `cost_anomaly_tools.py` | `get_anomalies` | `max_results` | 71 |
| `cost_anomaly_tools.py` | `get_anomaly_subscriptions` | `max_results` | 264 |
| `cost_comparison_tools.py` | `get_cost_category_definitions` | `max_results` | 107 |
| `cost_comparison_tools.py` | `list_cost_categories` | `max_results` | 204 |
| `cost_comparison_tools.py` | `list_cost_allocation_tags` | `max_results` | 327 |
| `cost_explorer_tools.py` | `get_cost_forecast` | `max_results` | 153 |
| `cost_optimization_hub_tools.py` | `list_recommendations` | `max_results` | 116 |
| `free_tier_usage_tools.py` | `get_free_tier_usage` | `max_results` | 62 |
| `bcm_pricing_calculator_tools.py` | `list_batch_pricelist_import_jobs` | `max_results` | 70 |
| `bcm_pricing_calculator_tools.py` | `list_usage_type_filters` | `max_results` | 236 |
| `bcm_pricing_calculator_tools.py` | `list_workload_estimates` | `max_results` | 377 |
| `bcm_pricing_calculator_tools.py` | `batch_get_workload_estimate_usage` | `max_results` | 633 |
| `aws_pricing_tools.py` | `describe_services` | `max_results` | 94 |

### 2. 其他辅助文件

| 文件名 | 受影响的函数 | 参数名 | 行号 |
|--------|------------|-------|------|
| `aws_pricing_operations.py` | `get_service_codes` | `max_results` | 34 |
| `aws_pricing_operations.py` | `get_attribute_values` | `max_results` | 157 |
| `aws_pricing_operations.py` | `get_products` | `max_results` | 246 |
| `cost_explorer_operations.py` | `get_reservation_coverage` | `max_results` | 225 |
| `cost_optimization_hub_helpers.py` | `list_recommendation_summaries` | `max_results` | 61 |
| `cost_optimization_hub_helpers.py` | `get_recommendation_by_id` | `max_results` | 345 |

---

## 🔧 详细修复步骤

### Phase 1: 准备工作

#### 1.1 创建通用解析函数

在 `awslabs/billing_cost_management_mcp_server/utilities/` 目录下创建或更新 `type_parsers.py`:

```python
"""Type parsing utilities for handling parameter type conversions."""

import logging
from typing import Optional, Union

logger = logging.getLogger(__name__)


def parse_int_param(
    param: Optional[Union[str, int]],
    function_name: str,
    param_name: str = "int_param",
    default: Optional[int] = None
) -> Optional[int]:
    """解析整数参数,支持字符串和整数输入.

    Args:
        param: 字符串或整数
        function_name: 调用此函数的函数名(用于日志)
        param_name: 参数名(用于日志)
        default: 默认值(当 param 为 None 时返回)

    Returns:
        解析后的整数或 None

    Raises:
        ValueError: 参数格式无效
    """
    if param is None:
        return default

    # 🔍 调试日志: 记录接收到的类型
    logger.info(
        "🔍 [%s] %s type: %s, value: %s",
        function_name,
        param_name,
        type(param).__name__,
        str(param)
    )

    # ⚡ 智能处理: 如果是 int,直接使用
    if isinstance(param, int):
        return param

    # ⚠️ 如果是字符串,尝试转换
    if isinstance(param, str):
        logger.warning(
            "⚠️ [%s] Received string for %s instead of int! Auto-converting...",
            function_name,
            param_name
        )
        try:
            return int(param)
        except ValueError as e:
            logger.error(
                "❌ [%s] Invalid integer format for %s: %s",
                function_name,
                param_name,
                str(e)
            )
            raise ValueError(f"Invalid integer format for {param_name}: {e}")

    # 其他类型
    raise ValueError(
        f"{param_name} must be string or int, got {type(param).__name__}"
    )
```

#### 1.2 创建测试文件

创建 `tests/utilities/test_type_parsers.py`:

```python
"""Tests for type parsing utilities."""

import pytest
from awslabs.billing_cost_management_mcp_server.utilities.type_parsers import parse_int_param


def test_parse_int_param_int():
    """测试整数输入"""
    result = parse_int_param(50, "test_func", "max_results")
    assert result == 50


def test_parse_int_param_string():
    """测试字符串输入"""
    result = parse_int_param("50", "test_func", "max_results")
    assert result == 50


def test_parse_int_param_none():
    """测试 None 输入"""
    result = parse_int_param(None, "test_func", "max_results")
    assert result is None


def test_parse_int_param_none_with_default():
    """测试 None 输入带默认值"""
    result = parse_int_param(None, "test_func", "max_results", default=100)
    assert result == 100


def test_parse_int_param_invalid_string():
    """测试无效字符串"""
    with pytest.raises(ValueError, match="Invalid integer format"):
        parse_int_param("invalid", "test_func", "max_results")


def test_parse_int_param_invalid_type():
    """测试无效类型"""
    with pytest.raises(ValueError, match="must be string or int"):
        parse_int_param(12.5, "test_func", "max_results")
```

---

### Phase 2: 修改 Tools 文件

#### 2.1 修改原则

对于每个包含 `max_results: Optional[int]` 的函数:

1. **修改参数类型**:
   ```python
   # 修改前
   max_results: Optional[int] = None

   # 修改后
   from typing import Union
   max_results: Optional[Union[str, int]] = None
   ```

2. **添加导入**:
   ```python
   from ..utilities.type_parsers import parse_int_param
   ```

3. **添加参数解析逻辑** (在使用 max_results 之前):
   ```python
   # 解析 max_results
   parsed_max_results = parse_int_param(
       max_results,
       "function_name",  # 替换为实际函数名
       "max_results"
   )

   # 使用解析后的值
   if parsed_max_results:
       request_params['maxResults'] = parsed_max_results
   ```

---

#### 2.2 具体修改清单

##### 优先级 P0 (主工具文件 - 立即修复)

- [ ] **compute_optimizer_tools.py**
  - [ ] `compute_optimizer()` 函数 (行 79)
  - [ ] 所有子函数中的 max_results 使用

- [ ] **cost_optimization_hub_tools.py**
  - [ ] `list_recommendations()` 函数 (行 116)

- [ ] **cost_anomaly_tools.py**
  - [ ] `get_anomalies()` 函数 (行 71)
  - [ ] `get_anomaly_subscriptions()` 函数 (行 264)

- [ ] **cost_explorer_tools.py**
  - [ ] `get_cost_forecast()` 函数 (行 153)

##### 优先级 P1 (次要工具文件 - 本周修复)

- [ ] **cost_comparison_tools.py**
  - [ ] `get_cost_category_definitions()` 函数 (行 107)
  - [ ] `list_cost_categories()` 函数 (行 204)
  - [ ] `list_cost_allocation_tags()` 函数 (行 327)

- [ ] **free_tier_usage_tools.py**
  - [ ] `get_free_tier_usage()` 函数 (行 62)

- [ ] **bcm_pricing_calculator_tools.py**
  - [ ] `list_batch_pricelist_import_jobs()` 函数 (行 70)
  - [ ] `list_usage_type_filters()` 函数 (行 236)
  - [ ] `list_workload_estimates()` 函数 (行 377)
  - [ ] `batch_get_workload_estimate_usage()` 函数 (行 633)

- [ ] **aws_pricing_tools.py**
  - [ ] `describe_services()` 函数 (行 94)

##### 优先级 P2 (辅助文件 - 下周修复)

- [ ] **aws_pricing_operations.py**
  - [ ] `get_service_codes()` 函数 (行 34)
  - [ ] `get_attribute_values()` 函数 (行 157)
  - [ ] `get_products()` 函数 (行 246)

- [ ] **cost_explorer_operations.py**
  - [ ] `get_reservation_coverage()` 函数 (行 225)

- [ ] **cost_optimization_hub_helpers.py**
  - [ ] `list_recommendation_summaries()` 函数 (行 61)
  - [ ] `get_recommendation_by_id()` 函数 (行 345)

---

### Phase 3: 验证和测试

#### 3.1 单元测试

为每个修改的函数添加测试:

```python
@pytest.mark.asyncio
async def test_compute_optimizer_max_results_int():
    """测试整数类型的 max_results"""
    result = await compute_optimizer(
        ctx=mock_context,
        operation="get_ec2_instance_recommendations",
        max_results=50  # 整数
    )
    assert result['status'] == 'success'


@pytest.mark.asyncio
async def test_compute_optimizer_max_results_string():
    """测试字符串类型的 max_results"""
    result = await compute_optimizer(
        ctx=mock_context,
        operation="get_ec2_instance_recommendations",
        max_results="50"  # 字符串
    )
    assert result['status'] == 'success'


@pytest.mark.asyncio
async def test_compute_optimizer_max_results_invalid():
    """测试无效的 max_results"""
    result = await compute_optimizer(
        ctx=mock_context,
        operation="get_ec2_instance_recommendations",
        max_results="invalid"
    )
    assert result['status'] == 'error'
    assert 'validation_error' in result['error_type']
```

#### 3.2 集成测试

在部署后进行实际测试:

```bash
# 测试 1: 整数参数
curl -X POST http://<gateway-url>/mcp \
  -H "Content-Type: application/json" \
  -d '{
    "method": "tools/call",
    "params": {
      "name": "billing-cost-management-mcp-production___compute-optimizer",
      "arguments": {
        "operation": "get_ec2_instance_recommendations",
        "target_account_id": "724652712935",
        "max_results": 50
      }
    }
  }'

# 测试 2: 字符串参数 (模拟 Gateway 序列化)
curl -X POST http://<gateway-url>/mcp \
  -H "Content-Type: application/json" \
  -d '{
    "method": "tools/call",
    "params": {
      "name": "billing-cost-management-mcp-production___compute-optimizer",
      "arguments": {
        "operation": "get_ec2_instance_recommendations",
        "target_account_id": "724652712935",
        "max_results": "50"
      }
    }
  }'
```

---

## 📝 示例修改 (compute_optimizer_tools.py)

### 修改前
```python
@compute_optimizer_server.tool(
    name='compute-optimizer',
    description="""...""",
)
async def compute_optimizer(
    ctx: Context,
    target_account_id: Optional[str] = None,
    operation: str = None,
    max_results: Optional[int] = None,  # ❌ 仅支持 int
    filters: Optional[str] = None,
    account_ids: Optional[str] = None,
    next_token: Optional[str] = None,
) -> Dict[str, Any]:
    try:
        # ...
        # 直接使用 max_results
        if max_results:
            request_params['maxResults'] = int(max_results)  # ❌ 可能失败
```

### 修改后
```python
from typing import Union
from ..utilities.type_parsers import parse_int_param

@compute_optimizer_server.tool(
    name='compute-optimizer',
    description="""...""",
)
async def compute_optimizer(
    ctx: Context,
    target_account_id: Optional[str] = None,
    operation: str = None,
    max_results: Optional[Union[str, int]] = None,  # ✅ 支持 str 和 int
    filters: Optional[str] = None,
    account_ids: Optional[str] = None,
    next_token: Optional[str] = None,
) -> Dict[str, Any]:
    try:
        # ...

        # ✅ 智能解析 max_results
        parsed_max_results = parse_int_param(
            max_results,
            "compute_optimizer",
            "max_results"
        )

        # 传递给子函数或使用
        if operation == 'get_ec2_instance_recommendations':
            return await get_ec2_instance_recommendations(
                ctx, co_client, parsed_max_results, filters, account_ids, next_token
            )
```

---

## ⚠️ 注意事项

### 1. 参数描述更新

修改函数签名后,**必须**更新参数描述:

```python
async def compute_optimizer(
    ctx: Context,
    target_account_id: Optional[str] = None,
    operation: str = None,
    max_results: Optional[Union[str, int]] = None,  # ✅ 修改类型
    # ...
) -> Dict[str, Any]:
    """Retrieves recommendations from AWS Compute Optimizer.

    Args:
        ctx: The MCP context
        target_account_id: Target AWS account ID
        operation: The operation to perform
        max_results: Maximum number of results (1-100). Accepts string or integer.  # ✅ 更新描述
        # ...
    """
```

### 2. 子函数传递

如果主函数调用子函数,确保传递的是**解析后的值**:

```python
# ✅ 正确
parsed_max_results = parse_int_param(max_results, "main_func", "max_results")
await sub_function(ctx, client, parsed_max_results, ...)

# ❌ 错误
await sub_function(ctx, client, max_results, ...)  # 可能传递未解析的字符串
```

### 3. 错误处理

在 tool 函数的异常处理中添加 ValueError 处理:

```python
except ValueError as e:
    # 参数验证错误
    return format_response(
        'error',
        {'error_type': 'validation_error'},
        f'Invalid parameter: {str(e)}'
    )
```

### 4. 日志记录

添加调试日志以便问题排查:

```python
ctx_logger = get_context_logger(ctx, __name__)
await ctx_logger.info(
    f"🔍 Parsing max_results: type={type(max_results).__name__}, value={max_results}"
)
```

---

## ✅ 验证清单

### 代码修改
- [ ] 创建 `utilities/type_parsers.py`
- [ ] 创建 `tests/utilities/test_type_parsers.py`
- [ ] 修改所有 P0 优先级文件
- [ ] 修改所有 P1 优先级文件
- [ ] 修改所有 P2 优先级文件
- [ ] 更新所有函数的参数描述

### 测试验证
- [ ] 单元测试通过
- [ ] 类型解析测试通过
- [ ] 本地启动测试通过
- [ ] 集成测试 - 整数参数
- [ ] 集成测试 - 字符串参数
- [ ] 集成测试 - 无效参数

### 部署验证
- [ ] 构建镜像成功
- [ ] 推送到 ECR 成功
- [ ] 更新 Runtime 成功
- [ ] 刷新 Gateway
- [ ] 生产环境验证通过

### 文档更新
- [ ] README 更新
- [ ] API 文档更新
- [ ] CHANGELOG 更新

---

## 📅 执行计划

### 第 1 天 (今天)
- ✅ 创建修复计划文档
- [ ] 创建 `type_parsers.py` 工具函数
- [ ] 创建单元测试
- [ ] 修复 P0 优先级文件 (compute_optimizer_tools.py)
- [ ] 本地测试验证

### 第 2 天
- [ ] 修复 P0 优先级文件 (cost_optimization_hub_tools.py, cost_anomaly_tools.py)
- [ ] 修复 P1 优先级文件 (cost_comparison_tools.py, free_tier_usage_tools.py)
- [ ] 单元测试验证

### 第 3 天
- [ ] 修复 P1 优先级文件 (bcm_pricing_calculator_tools.py, aws_pricing_tools.py)
- [ ] 修复 P2 优先级文件 (所有辅助文件)
- [ ] 完整集成测试

### 第 4 天
- [ ] 构建和部署
- [ ] 生产环境验证
- [ ] 文档更新

---

## 🔗 参考资料

- [MCP Gateway 适配改造分析](../20260117_MCP-Gateway-适配改造分析/02_改造步骤指南.md)
- [RISP MCP 参数类型问题分析](../20260120_RISP_MCP参数类型问题最终分析.md)
- [Filter Expression 分析](../20260120_filter_expression分析/问题总结与修复方案.md)
- [RISP Handler 修复代码](../../src/costq-risp-mcp-server/handlers/sp_handler.py)

---

## 📊 统计信息

- **受影响文件总数**: 14 个主文件 + 3 个辅助文件
- **受影响函数总数**: ~25 个
- **预计修改时间**: 3-4 天
- **风险等级**: 低 (修改模式统一,测试覆盖完善)
